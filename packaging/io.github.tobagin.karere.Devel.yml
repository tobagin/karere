app-id: io.github.tobagin.karere.Devel
runtime: org.gnome.Platform
runtime-version: '48'
sdk: org.gnome.Sdk
command: karere

finish-args:
  # Network access for WhatsApp Web
  - --share=network
  # IPC access for desktop integration
  - --share=ipc
  # Wayland socket for modern display server
  - --socket=wayland
  # X11 fallback for compatibility
  - --socket=fallback-x11
  # Audio for notification sounds
  - --socket=pulseaudio
  # Desktop notifications
  - --talk-name=org.freedesktop.Notifications
  # Application data directory (separate from production)
  - --persist=.var/app/io.github.tobagin.karere.Devel
  # Access to user downloads for file sharing
  - --filesystem=xdg-download
  # WebKit process sandbox permissions
  - --allow=devel
  # Hardware acceleration
  - --device=dri
  # Development debugging permissions
  - --env=G_MESSAGES_DEBUG=all
  # Enable spell checking support
  - --talk-name=org.freedesktop.portal.SpellCheck

cleanup:
  - /include
  - /lib/pkgconfig
  - /lib/cmake
  - /man
  - /share/doc
  - /share/gtk-doc
  - /share/man
  - /share/pkgconfig
  - /share/vala
  - /share/aclocal
  - /share/devhelp
  - '*.la'
  - '*.a'

modules:
  - name: appstream
    buildsystem: meson
    config-opts:
      - -Dstemming=false
      - -Dapidocs=false
      - -Dinstall-docs=false
    sources:
      - type: git
        url: https://github.com/ximion/appstream.git
        tag: v1.0.5
        commit: 8af1b8b61c5682751cec21c5b644cb1b8d20d5c6

  - name: blueprint-compiler
    buildsystem: meson
    sources:
      - type: git
        url: https://gitlab.gnome.org/jwestman/blueprint-compiler.git
        tag: v0.18.0
        commit: 07c9c9df9cd1b6b4454ecba21ee58211e9144a4b

  - name: karere
    buildsystem: meson
    config-opts:
      - -Dtests=true
      - -Dprofile=development
    sources:
      - type: dir
        path: ..
    post-install:
      - desktop-file-validate /app/share/applications/io.github.tobagin.karere.Devel.desktop
      - appstreamcli validate --no-net /app/share/metainfo/io.github.tobagin.karere.Devel.appdata.xml
      - glib-compile-schemas /app/share/glib-2.0/schemas