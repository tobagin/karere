# KARE-016 Step 1 — Baseline dev Flatpak run (n5air)

Date: 2026-08-17, host n5air, Fedora 44 (7.1.8-200.fc44), GNOME Shell Wayland + Xwayland :0, NVIDIA hybrid, org.gnome.Platform 50. Reporter env unknown — documented below.

## Build
`FLATPAK_BUILDER_EXTRA_ARGS="--disable-rofiles-fuse" ./build.sh --dev` succeeds; without flag, `rofiles-fuse` fails `exit_status: 1024` on this host (bwrap/fuse). Devel Flatpak installed as `io.github.tobagin.karere.Devel` at commit `4ab0412e3bde68461a509c0953a3ff42180f433a79ce8016e6f6191d8b962210`. Both stable and Devel manifests enforce `--socket=wayland`, `--socket=fallback-x11`, `--env=GSK_RENDERER=gl` (verified by `stable_and_devel_flatpak_graphics_policy_stays_synchronized`).

## Startup log — GLArea context line
Full logs captured to `/tmp/kare016-logs/` (also see `cargo test --test gl_context_startup` fixture).

**Host Wayland visible window** (`DISPLAY=:0 WAYLAND_DISPLAY=wayland-0 flatpak run io.github.tobagin.karere.Devel --debuglevel=info --url about:blank`, `start-in-background=false`):
```
[INFO  karere] Karere starting
[INFO  karere::cef_runtime] CEF context initialized / CEF initialized
[INFO  karere::web_view::imp] GLArea context ready: api=GLAPI(GLES) version=3.2
[INFO  karere::web_view::imp] accel_osr: GL_VENDOR=NVIDIA Corporation
[WARN  karere::web_view::imp] accel_osr: NVIDIA driver detected — GPU rendering is unsupported (#167); using software rendering
[INFO  karere::web_view::imp] accel_osr: requested=false enabled=false
[INFO  karere::handlers::life_span] browser created
[INFO  karere::web_view::imp] browser spawned (foreground=true)
```
With `--env=GDK_SCALE=2` the same `GLArea context ready: api=GLAPI(GLES) version=3.2` line appears (harness uses `flatpak run --env=...` to force sandbox env).

**Xvfb/GLES fixture** (`dbus-run-session -- xvfb-run -a -s '-screen 0 1280x800x24' cargo test --test gl_context_startup`):
- Legacy desktop-GL contract correctly fails: `GLArea realize error: Unable to create a GL context` and browser not spawned.
- Fixed GLES contract: `GLArea context ready: api=GLAPI(GLES) version=3.2` + `browser spawned` for visible window, and same after `present-window` for prewarmed (`start-in-background=true`) window. Uses llvmpipe with `MESA_GL_VERSION_OVERRIDE=2.1 MESA_GLES_VERSION_OVERRIDE=3.2 LIBGL_ALWAYS_SOFTWARE=1 GALLIUM_DRIVER=llvmpipe KARERE_GPU_OSR=0 GDK_BACKEND=x11 GSK_RENDERER=gl`.

## Repro knobs on this host
- Sandbox env is forced via `flatpak run --env=KEY=VAL` (verified `FOO=bar` propagates; `GDK_SCALE=2` works). This is the documented harness mechanism.
- X11 repro under Xvfb works via the manifest's `--socket=fallback-x11`. On this host, the Flatpak forwards `DISPLAY` only when the host session exports it before `flatpak run` (`DISPLAY=:0`/`WAYLAND_DISPLAY=wayland-0` from `systemctl --user show-environment`). For deterministic CI without a host session, the native binary fixture (`cargo test --test gl_context_startup`) is canonical.
- Host deviation from reporter: reporter environment unknown; this host is Fedora Wayland+NVIDIA hybrid with `xwayland-native-scaling`/`scale-monitor-framebuffer` experimental features, while harness must not depend on session type — it explicitly exports display vars and supports both GDK_SCALE=1 and GDK_SCALE=2 matrix under Xvfb and Wayland.

## CEO constraints
`GSK_RENDERER=gl` preserved, no driver/GL backend overrides during capture, no real WhatsApp login or chat content captured (synthetic `about:blank` only).

