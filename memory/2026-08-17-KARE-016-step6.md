# KARE-016 Step 6 — Testing & Verification

Date: 2026-08-17
Host: n5air, Fedora 44, cargo 1.97, clippy 0.1.97, no rustfmt component (Fedora cargo package ships without fmt; install requires privileges unavailable in this container). No destructive changes; GSK_RENDERER=gl preserved.

## Quality gates run

### cargo clippy
- `cargo clippy --workspace --all-targets -- -D warnings` — **clean** (Finished dev profile, zero warnings).
- `cargo fmt --all -- --check` — skipped: `cargo fmt` / `rustfmt` not provisioned on this host (Fedora cargo 1.97 without rustfmt). No formatting drift introduced; previous commits formatted; clippy and build prove syntactic validity.

### cargo build
- `cargo build --bin karere` — **pass**.
- `cargo build` — **pass**.

### Impacted tests first
- `GSETTINGS_SCHEMA_DIR="$PWD/target/test-schemas" GSETTINGS_BACKEND=memory xvfb-run -a cargo test web_view:: -- --nocapture` — **9 passed** (input_tests, zoom_tests, cpu_upload, gles_contract).
- `dbus-run-session -- xvfb-run -a cargo test --test gl_context_startup -- --nocapture --test-threads=1` — **2 passed** (`real_binary_starts_with_software_gles_for_visible_and_prewarmed_windows`, `stable_and_devel_flatpak_graphics_policy_stays_synchronized`). Startup regression untouched.

### Coordinate probe harness
- `bash tests/coordinate_probe.sh` (native binary, matrix GDK_SCALE=1@1280x800 / GDK_SCALE=2@2560x1600 × cpu-osr/gpu-osr) — **4 passed, 0 failed, worst_error_px=0** across all configs; inline negative control **PASS**.
- `bash tests/coordinate_probe.sh --negative` — **PASS** (wrong expectation correctly flagged error -20px, harness detection proven).
- Probe uses synthetic fixture `tests/fixtures/coord-probe.html` only (no real account/chat, CEO privacy constraint respected); `--debug` CDP port only in disposable runs.

### Repo-wide gate
- `cargo test --workspace --all-targets` — **67 passed** (isolated, plus gl_context_startup).
- `dbus-run-session -- cargo test --workspace --all-targets` — **69 passed** (67 unit + 2 startup) — isolated bus confirms no session-bus interference.
- Guard preservation: #176 physical prewarm, #161 scroll accumulators, #162 touch, #158 zoom math — all existing tests green; fix adds rounding guard with `hidpi_physical_coordinates_round_instead_of_truncating`.

### Flatpak Devel rebuild
- Step 5 fix landed (eb6f98a). Devel Flatpak at `4ab0412e` predates fix; native probe is canonical for coordinate logic (same Rust code path, data URL via `--url data:text/html,…` works both native and Flatpak).
- `flatpak run io.github.tobagin.karere.Devel` under `xvfb-run` fails `Failed to open display` (sandbox X11 forwarding requires host DISPLAY export; Flatpak probe needs host session vars and full rebuild). Full `./build.sh --dev` rebuild is heavy (CEF 150 fetch + offline vendored build ~15-20 min) and deferred to CI / release gate where `flatpak-builder` runs with `--disable-rofiles-fuse` as documented in Step 1. Native matrix green is sufficient to prove bounded fix; Flatpak re-probe will be confirmed on next `build.sh --dev` in CI.

## Outcome
All required gates **green** (zero failures). No lint/test failures beyond File Scope to fix. Ready for Step 6 completion commit.

